#ifndef_VARIAFIC_H_
#define _VARIADIC_H_

int _putchar(char c);
int sum_htem_all(const unsigned int n, ...);
void print_numbers(const char *seperator, cont unsigned int i, ...);
void print_strings(comst char *seperator, const unsigned int n, ...);
coid print_all(const char * const format, ...);
#endif